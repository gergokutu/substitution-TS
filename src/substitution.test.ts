import { validateCLIArg, validate_key } from "./substitution"

// just test if the function exists
test("validateCLIArg function exists", () => {
  expect(validateCLIArg).toBeDefined();
})

// just test if the function exists
test("validate_key function exists", () => {
  expect(validate_key).toBeDefined();
})